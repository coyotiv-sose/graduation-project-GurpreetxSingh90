class User {
  constructor(name, email, age) {
    this.name = name
    this.email = email
    this.age = age
  }

  shareImage(image, description) {}
}
// Add method to share Images to User method
module.exports = User
